/**
 * Same frame as HeroSection: `max-w-6xl` centered. Pair with `px-4 sm:px-6` on the `<section>`
 * (padding outside the shell, like the hero) so titles line up with the main name.
 */
export const sectionShellClassName = "mx-auto w-full max-w-6xl";
